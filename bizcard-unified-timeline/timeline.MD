这是一份关于 **BizCard 2.0 时间流 (Unified Timeline)** 的通用改造方案概述。

这份文档旨在为 UI 设计团队和研发团队提供一个**宏观的架构指引**，明确我们将如何把现在的“列表页”重构为“智能流”。

---

# BizCard 2.0 Timeline Transformation Overview

**核心目标：** 将 App 的核心交互模式从 **“查找文件的列表 (File List)”** 升级为 **“管理动态的流 (Activity Stream)”**。

---

## 1. 设计哲学 (Design Philosophy)

* **单一视口 (Single Viewport):** 摒弃多 Tab 割裂的体验，所有核心数据（会议、通话、笔记、联系人交互）都在**同一条**纵向时间轴上呈现。
* **面向对象 (Object-Oriented UI):** 用户的每一次筛选（搜人、搜日期、搜类型），本质上都是在改变这条流的 **Scope (观察范围)**，而不是跳转到新页面。
* **行动优先 (Action-First):** 无论流的内容如何变化，待办事项 (Action Items) 永远吸附在顶部，确保执行力。

---

## 2. 界面架构：四层模型 (The 4-Layer Architecture)

我们将首页在纵向上划分为四个逻辑层级，实现“控制与内容”的分离。

### Layer 1: Global Control (全局控制层)

* **定位:** 顶部的常驻操作区，负责定义流的**范围**。
* **左侧:** **[Avatar]**。点击滑出左侧抽屉 (Settings Drawer)，配置设备、名片信息、Agent 设置。
* **中间:** **[Search / Filter Bar]**。
* *输入:* 搜索关键词。
* *筛选:* 快速切换 `All` | `Meetings` | `Agent Calls` | `Date`。
* *选人:* 通过右侧图标唤起 **“通讯录列表”** 弹窗。


* **右侧:** **[Share/QR]**。快速展示我的名片码。

### Layer 2: Context Header (语境头图)

* **定位:** 动态展示当前筛选对象的“静态资料”。**默认隐藏**，仅在筛选生效时展开。
* **场景 A (筛选了 Kevin):** 展开展示 Kevin 的 Profile Header (头像 + Title + Email/LinkedIn 入口)。点击可查看全屏详情。
* **场景 B (筛选了 Meetings):** 展示类型图标 + 清除筛选按钮 `(x)`。

### Layer 3: Action Hub (行动中枢)

* **定位:** **吸顶 (Sticky)** 的任务通知条。
* **逻辑:** 内容随流而动。
* *Global:* 显示所有人的待办。
* *Filtered (e.g. Kevin):* 只显示与 Kevin 相关的待办。


* **交互:** 可折叠/展开 checklist，勾选即完成。

### Layer 4: The Stream (垂直内容流)

* **定位:** 核心浏览区，纵向无限滚动。
* **数据源:** 聚合了 Hardware Recordings (线下) + Agent Calls (线上) + Manual Notes (手动)。

---

## 3. 卡片设计逻辑 (Card Design System)

时间流中的卡片不再是简单的文件名，而是**结构化的资产卡片**。

### A. Meeting Card (会议卡片)

* **Header:** 时间 + 标题 (AI 生成的主题，如 "RWA Proposal Discussion")。
* **Tags:** `#HighPriority` `#Contract` (视觉降噪，用颜色区分)。
* **Body:** 2-3 行核心摘要。
* **Footer:** 关联联系人头像 (点击可聚焦该人)。

### B. Agent Call Card (通话卡片)

* **Visual:** 明显的 📞 电话图标区分。
* **Content:** 对方的 **Leave Message (留言)** + AI 建议的动作 (如 "Call Back").

### C. Action Log (动作日志)

* **Content:** 当任务被勾选完成后，流中会插入一条灰色的小记录：`✔ Completed: Sent email to Kevin`。形成闭环反馈。

---

## 4. 关键交互流 (Key Interactions)

### 场景一：日常浏览 (The Morning Check)

1. 打开 App，看到 **Layer 3 Action Hub** 提示 "5 Pending Actions"。
2. 向下滑动 **Layer 4 Timeline**，浏览昨天发生的会议和未接电话。
3. 点击右下角 **FAB**，录入一条今天的备忘。

### 场景二：聚焦某人 (The Deep Dive)

1. 在 Timeline 中点击 "Kevin" 的头像，或者在顶部搜索 "Kevin"。
2. **视觉变化:**
* Timeline 瞬间过滤，只保留 Kevin 的历史记录。
* **Layer 2 展开:** 显示 Kevin 的职位、公司、邮箱入口。
* **Layer 3 更新:** 只显示 "Pending Actions with Kevin"。


3. **操作:** 在 Layer 2 点击邮箱图标发邮件，或在 Layer 3 勾选完成任务。

---

## 5. 硬件与 AI 协同 (Synergy)

* **Input (输入):** 硬件录音结束后，数据自动推送到 Timeline 顶部，状态为 "Processing..." -> "Ready"。
* **Link (关联):** 新录入的卡片如果未关联联系人，显示显眼的 **[Link Contact]** 按钮，点击后 AI 推荐联系人列表。
* **Recall (反查):** 在硬件上按住语音查询，结果来源于 Timeline 数据的检索。

---

## 6. 开发实施路径建议

* **Phase 1 (数据层改造):** 先在现有 App 中打通 Meeting Summary -> Action Items -> Contact 的数据关联。
* **Phase 2 (UI 层重构):** 引入 "Sticky Header" 和 "Vertical Scroll View" 组件，搭建首页框架。
* **Phase 3 (动态交互):** 实现 Layer 2 (Profile Header) 的展开/收起动画和筛选逻辑。

---

## 附：可落地交互 PRD（研发/设计对齐用）

- **Unified Timeline App 交互 PRD（可落地版）**：`bizcard-unified-timeline/UnifiedTimeline_AppInteraction_PRD.md`