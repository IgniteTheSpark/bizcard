/**
 * ChatPage — M0 stub. M2 builds out:
 *  - SessionSidebar (Claude-style, desktop persistent / mobile drawer)
 *  - MessageList + MessageBubble + AssetCardInChat
 *  - useChat (SSE)
 *  - history replay from /api/sessions/:id/messages
 *  - PrecipitateButton
 */
export function ChatPage() {
  return (
    <div className="p-eu-lg">
      <p className="text-eu-text-mid text-eu-sm">
        Chat page — coming in M2 (SSE chat + session sidebar + cards-in-chat).
      </p>
    </div>
  );
}
