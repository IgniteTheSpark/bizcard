/**
 * NotificationPage — M0 stub. M6 builds out:
 *  - useNotifications (history fetch + SSE subscription)
 *  - NotificationItem list
 *  - mark-read / dismiss interactions
 */
export function NotificationPage() {
  return (
    <div className="p-eu-lg">
      <p className="text-eu-text-mid text-eu-sm">
        Notifications — coming in M6 (toast + history + SSE push).
      </p>
    </div>
  );
}
