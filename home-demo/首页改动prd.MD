# BizCard App 首页产品需求文档 (PRD)

## 1. 产品概述

BizCard 是一款 AI 驱动的社交利器，通过 3.8mm 极致轻薄的硬件卡片与软件 App 联动，实现 **Show（展示）**、**Exchange（交换）** 与 **Capture（捕捉）** 的社交全链路闭环。首页作为产品的"核心仪表盘"，负责身份管理、即时反馈社交 ROI 并提供关键交互入口。

---

## 2. 核心交互逻辑：四段式布局

首页采用纵向四段式布局，确保信息层级清晰且操作高效。

### 2.1 第一段：身份与设备中枢 (Identity & Gear Hub)

#### 功能描述

管理用户的数字身份与物理硬件显示状态，通过堆叠卡片实现切换。

#### 堆叠卡片交互

```plaintext
┌─────────────────────────────────────────────┐
│  ┌─────────────────────────────────────┐    │
│  │ 👤 Dr. Evelyn Reed · Designer       │    │  ← 背景卡片（数字名片）
│  └─────────────────────────────────────┘    │     只展示关键信息
│  ┌─────────────────────────────────────┐    │
│  │   ⚡85% | Connected | 📶            │    │  ← 前景卡片（硬件）
│  │                                     │    │
│  │   ┌─────────────────────────────┐   │    │
│  │   │   BizCard                   │   │    │     E-ink 屏幕居中显示
│  │   │   Ready to Capture          │   │    │
│  │   └─────────────────────────────┘   │    │
│  │                                     │    │
│  │      [🔄 Sync]    [⚙️ Settings]     │    │  ← 按钮沉底，2个按钮
│  └─────────────────────────────────────┘    │
└─────────────────────────────────────────────┘

点击卡片切换（带动画效果）↓

┌─────────────────────────────────────────────┐
│  ┌─────────────────────────────────────┐    │
│  │ ⚡85% | Connected | 📶              │    │  ← 背景卡片（硬件状态）
│  └─────────────────────────────────────┘    │
│  ┌─────────────────────────────────────┐    │
│  │  👤 Dr. Evelyn Reed                 │    │  ← 前景卡片（数字名片）
│  │     Product Designer                │    │     不显示硬件信息
│  │     email@example.com               │    │     （因为硬件信息在背景卡展示）
│  │                                     │    │
│  │  💡 UX Research  💡 UI Design       │    │  ← Tags 用 icon 区分类型
│  │  🎯 Prototyping                     │    │     💡=Skills  🎯=Needs
│  │                                     │    │
│  │  [✏️ Edit] [🤖 Agent] [👁️ Preview]  │    │  ← 按钮沉底，3个简化按钮
│  └─────────────────────────────────────┘    │
└─────────────────────────────────────────────┘
```

#### 卡片切换动效

- **退出动画**：当前前景卡片向上淡出缩小
- **进入动画**：新卡片从下方滑入放大
- **背景卡片**：平滑渐变切换
- **过渡曲线**：使用弹性曲线 `cubic-bezier(0.34, 1.56, 0.64, 1)` 增强质感

#### 背景卡片信息规则

| 背景卡片类型 | 展示信息 |
| --- | --- |
| **数字名片在后** | 头像 + 姓名 · Title · 公司（单行紧凑展示） |
| **硬件卡片在后** | ⚡电量 \| 连接状态 \| 📶信号 |

#### 功能按钮（根据前景卡片类型变化）

| 前景卡片 | 按钮配置 | 说明 |
| --- | --- | --- |
| **数字名片** | `[✏️ Edit]` `[🤖 Agent]` `[👁️ Preview]` | 3 个按钮，文字简化 |
| **硬件卡片** | `[🔄 Sync]` `[⚙️ Settings]` | 2 个按钮，E-ink 需手动同步 |

#### Tags 展示规则

- **不再使用** `Skills:` 和 `Needs:` 标题
- 使用 **icon 区分类型**：
  - 💡 表示 **Skills**（技能/能提供的）
  - 🎯 表示 **Needs**（需求/正在找的）
- 所有 tags 在同一区域正常换行排列

#### 按钮位置规则

- 按钮始终 **沉底显示**（使用 `margin-top: auto`）
- 按钮与上方内容保持 **12px 以上间距**
- 两张卡片的按钮位置 **垂直对齐**，切换时不产生跳动

#### 按钮跳转逻辑

| 按钮 | 点击行为 |
| --- | --- |
| `Edit` | 进入名片编辑页 |
| `Agent` | 进入 Agent 测试页（预设流程） |
| `Preview` | 预览名片公开页面 |
| `Sync` | 同步 E-ink 屏幕显示内容 |
| `Settings` | 进入硬件设置页 |

---

### 2.2 第二段：运营展示位 (Marketing Card)

#### 功能描述

用于功能引导、硬件配件推荐或 AI 社交模版曝光。

#### 视觉规格

- 采用 **紧凑设计**，不抢夺主要内容视觉权重
- 背景支持毛玻璃（Glassmorphism）或品牌色渐变效果
- 高度适中，确保下方 Engagement 区域在首屏可见

#### 内容策略

| 硬件状态 | 推荐内容 |
| --- | --- |
| **已连接** | 功能升级、进阶玩法、AI 模版推荐 |
| **未连接** | 连接引导、硬件配件购买推荐 |

---

### 2.3 第三段：今日社交洞察 (Today's Engagement)

#### 功能描述

实时量化社交价值，并提供最近活动的快速回看。

#### 社交 ROI 看板 (Stats Row)

采用 **紧凑设计**，作为辅助信息展示，不抢夺视觉焦点。

| 指标 | 含义 | 图标 |
| --- | --- | --- |
| **Visits** | 今日有多少人访问了我的名片 | 👁️ |
| **Cards Added** | 今日我添加了多少别人的名片 | 🤝 |
| **Meetings** | 今日会面数量（Agent Call + 线下 Meeting） | 📍 |

```plaintext
+-----------------------------------------------+
|   👁️ 12      🤝 3       📍 2                  |
|   VISITS    CARDS ADDED  MEETINGS             |  ← 紧凑展示，字体较小
+-----------------------------------------------+
```

#### Stats 指标点击交互

| 指标 | 点击行为 |
| --- | --- |
| **👁️ Visits** | 跳转 Timeline → Activities Tab → 筛选访问类型 |
| **🤝 Cards Added** | 跳转 Contacts 页面 → 按添加时间排序 |
| **📍 Meetings** | 跳转 Timeline → **Meetings View**（增强版，含状态筛选） |

> **重要：** 📍 Meetings 指标是进入「会议记录列表」的快捷入口，用户可以在此查看所有录音的处理状态。

#### 今日动态预览 (Engagement Feed)

- **展示规则：** 紧凑布局，首屏至少展示 **2-3 条** 记录
- **数据类型：** 仅展示 **Meeting Card**、**Agent Card**、**Exchange Card**（不含小圆点类型）
- **排序规则：** 按时间倒序，**最近发生的在最上面**
- **点击行为：** 点击单条动态 → 跳转对应**详情页**
- **空状态处理：** 若无数据，在相同高度区域内展示插图引导

```plaintext
+-----------------------------------------------+
|  Today's Engagement                           |
|                                               |
|  🤝 Exchange with John Mitchell   11:30 AM › |  ← 最近的在上面
|  📍 Q4 Strategy Review            10:00 AM › |
|  🤖 Visitor #402 - Pricing         9:15 AM › |
|                                               |
|              [View All History ›]             |
+-----------------------------------------------+
```

#### 列表项设计

- 使用 **紧凑的行高**（padding: 10px 0）
- 标题使用 **text-overflow: ellipsis** 防止长文本破坏布局
- 箭头使用 `›` 而非 `→`，更轻盈

#### 跳转逻辑

| 操作 | 跳转目标 |
| --- | --- |
| 点击 `View All History` | Timeline 页面 → **Activities Tab** → 定位到 **Today** |
| 点击单条动态 | 对应的**详情页**（Meeting 详情 / Agent Call 详情 / 联系人名片页） |
| 点击 Stats Row「📍 Meetings」 | Timeline 页面 → **Meetings View**（增强版，含状态筛选） |

---

### 2.4 第四段：悬浮操作条 (Sticky Bar)

#### 位置

固定于 Tab Bar 上方，始终可见，不随内容滚动。

#### 布局

两个按钮 **5:5 平分** Sticky Bar 宽度：

```plaintext
+-----------------------------------------------+
|                                               |
|              (可滚动内容区)                    |
|                                               |
+===============================================+
|   [📷 Scan to Add]   |   [🔴 Start Capture]  |  ← Sticky Bar (固定)
+-----------------------------------------------+
||  Home  Timeline  Share  Contacts  Me        |  ← Tab Bar (固定)
+-----------------------------------------------+
```

#### 按钮功能

| 按钮 | 功能 | 状态逻辑 |
| --- | --- | --- |
| `[📷 Scan to Add]` | 开启摄像头扫描对方名片 | 始终可用 |
| `[🔴 Start Capture]` | 开启硬件录音及 AI 摘要 | 硬件已连接时可用；未连接时置灰 |

#### 未连接硬件时的交互

- `[🔴 Start Capture]` 按钮**置灰不可点击**
- 点击置灰按钮 → 跳转**硬件连接页面**，引导用户完成连接

> **注意：** Sticky Bar 不再展示电量和连接状态，因为该信息已在第一段的硬件卡片中展示。

---

## 3. 状态管理：硬件连接性逻辑

首页的交互状态高度依赖于硬件连接情况。

| 模块 | 硬件已连接 (Connected) | 硬件未连接 (Disconnected) |
| --- | --- | --- |
| **硬件预览（前景）** | 展示实时内容预览，状态栏绿色 | 预览区置灰，显示"Device Offline" |
| **硬件预览（背景）** | 展示 `⚡85% | Connected | 📶` | 展示 `⚠️ Disconnected` |
| **设备按钮** | `[Sync]` `[Settings]` | 同上，但操作可能受限 |
| **Sticky Bar** | `[🔴 Capture]` 正常可用 | `[🔴 Capture]` 置灰，点击跳转连接页 |
| **运营 Banner** | 功能升级、进阶玩法 | 连接引导、硬件配件推荐 |

---

## 4. 导航与全局规范

### 主导航栏 (Tab Bar)

`[Home]` `[Timeline]` `[Share]` `[Contacts]` `[Me]`

- Tab Bar **固定在底部**，不随内容滚动
- 当前页面对应的 Tab 高亮显示

### 页面联动

| 首页入口 | 跳转目标 |
| --- | --- |
| `View All History` | Timeline → Activities Tab → Today |
| 点击今日动态条目 | 对应详情页 |
| 点击 Stats「📍 Meetings」 | Timeline → Meetings View（含状态筛选） |
| `Agent` | Agent 测试页 |
| `Preview` | 名片公开预览页 |
| `Capture` (未连接) | 硬件连接引导页 |

### UI 风格

干净的奢侈科技感（Luxury Tech），结合 iOS 的毛玻璃效果与硬件的工业质感。

#### 设计原则

1. **紧凑布局** - 首屏展示尽可能多的关键信息
2. **视觉层次** - 卡片区域 > Engagement 列表 > Stats 数据
3. **一致性** - 两张卡片的按钮位置对齐，切换平滑
4. **动效克制** - 使用弹性曲线，动画时长 0.3-0.5s

---

## 5. 原型图例

```plaintext
+-----------------------------------------------+
|  ┌─────────────────────────────────────────┐  |
|  │  👤 Dr. Evelyn Reed · Designer · BizCard│  |  ← 背景卡片
|  └─────────────────────────────────────────┘  |
|  ┌─────────────────────────────────────────┐  |
|  │     ⚡85% | Connected | 📶              │  |  ← 前景卡片（硬件）
|  │                                         │  |
|  │     ┌─────────────────────────────┐     │  |
|  │     │      BizCard                │     │  |     E-ink 屏幕
|  │     │   Ready to Capture          │     │  |
|  │     └─────────────────────────────┘     │  |
|  │                                         │  |
|  │        [🔄 Sync]   [⚙️ Settings]        │  |  ← 按钮沉底
|  └─────────────────────────────────────────┘  |
|                                               |
|  ┌─────────────────────────────────────────┐  |
|  │  🎯 Unlock AI Social Templates      →   │  |  ← Marketing Card (紧凑)
|  └─────────────────────────────────────────┘  |
|                                               |
|  ┌─────────────────────────────────────────┐  |
|  │  👁️ 12      🤝 3       📍 2             │  |  ← Stats Row (紧凑)
|  │─────────────────────────────────────────│  |
|  │  Today's Engagement                     │  |
|  │                                         │  |
|  │  🤝 Exchange with John M    11:30 AM › │  |  ← 按时间倒序
|  │  📍 Q4 Strategy Review      10:00 AM › │  |
|  │  🤖 Visitor #402             9:15 AM › │  |
|  │                                         │  |
|  │           [View All History ›]          │  |
|  └─────────────────────────────────────────┘  |
|                                               |
+===============================================+
|   [📷 Scan to Add]   |   [🔴 Start Capture]  |  ← Sticky Bar (固定)
+-----------------------------------------------+
|  [Home]  [Timeline]  [Share]  [Contacts] [Me]|  ← Tab Bar (固定)
+-----------------------------------------------+
```

---

## 6. 更新记录

| 版本 | 日期 | 更新内容 |
| --- | --- | --- |
| v1.0 | - | 初版 PRD |
| v1.1 | 2026-01-12 | 硬件卡片按钮简化为 2 个（Sync, Settings） |
| v1.1 | 2026-01-12 | Profile 卡片按钮简化为 Edit, Agent, Preview |
| v1.1 | 2026-01-12 | Tags 移除标题，改用 icon 区分（💡Skills, 🎯Needs） |
| v1.1 | 2026-01-12 | Engagement 列表按时间倒序排列 |
| v1.1 | 2026-01-12 | 添加卡片切换动效说明 |
| v1.1 | 2026-01-12 | 强调按钮沉底显示规则 |
| v1.1 | 2026-01-12 | 整体布局紧凑化，确保首屏展示更多内容 |
| v1.1 | 2026-01-12 | Profile 前置时不显示硬件信息（避免重复） |
| v1.1 | 2026-01-12 | Tab Bar 和 Sticky Bar 固定不随内容滚动 |
